{
  "name": "gatekeeper",
  "private": true,
  "scripts": {
    "test": "lerna run test",
    "build": "lerna run build",
    "lint": "lerna run lint"
  },
  "devDependencies": {
    "lerna": "^3.13.4"
  },
  "dependencies": {
  }
}
