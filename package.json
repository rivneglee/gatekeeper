{
  "name": "gatekeeper",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap --npm-client=yarn",
    "test": "lerna run test",
    "build": "lerna run build",
    "lint": "lerna run lint"
  },
  "devDependencies": {
    "lerna": "^3.13.4"
  },
  "dependencies": {
  }
}
