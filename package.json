{
  "name": "gatekeeper",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap && lerna run build",
    "test": "lerna run test",
    "build": "lerna run build"
  },
  "devDependencies": {
    "@types/shortid": "^0.0.29",
    "lerna": "^3.13.4"
  },
  "dependencies": {
    "shortid": "^2.2.14"
  }
}
